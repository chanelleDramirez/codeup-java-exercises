package Shapes;

public class Rectangle {
    protected int length;
    protected int width;

    protected int side;
    public Rectangle(int length, int width) {
        this.length = length;
        this.width = width;
    }


        public Rectangle(int length, int width) {
            this.length = 10 + 10;
            this.width = 5 + 5;
        }
        public static getPerimeter(int length, int width) {
            perimeter = 2 * length + 2 * width;
            return ((2 * length) + (2 * width)
        }
        public static getArea(int length, int width) {
            area = length * width;
            return( width * length)
        }
    }

